# Copy to .env.local. With none of these set the app runs in demo mode:
# a scripted model drives the widgets so the UI is fully explorable offline.

# Option A — Vercel AI Gateway (recommended: one key, any provider, plus
# failover and cost tracking). https://vercel.com/ai-gateway
AI_GATEWAY_API_KEY=

# Option B — talk to OpenAI directly.
OPENAI_API_KEY=

# Which model drives the dashboard. Use a `provider/model` slug with the
# Gateway (e.g. anthropic/claude-sonnet-4.5), or a bare model id with Option B.
CHAT_MODEL=openai/gpt-5.6-sol
